name: reop_viewer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  oauth2: ^2.0.0
  flutter_secure_storage: ^4.2.1
  dartz: ^0.10.0
  flutter_hooks: ^0.18.0
  hooks_riverpod: ^0.14.0+5
  freezed_annotation: ^0.14.1
  webview_flutter: ^2.0.4
  flash: ^2.0.3
  auto_route:
    git:
      url: https://github.com/madtocc/auto_route_library.git
      ref: hotfix/flutter_2.2.3
      path: auto_route/
  dio: ^4.0.0
  material_design_icons_flutter: ^5.0.6295
  json_annotation: ^4.0.1
  sembast: ^3.0.2
  path_provider: ^2.0.1
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  auto_route_generator:
    git:
      url: https://github.com/madtocc/auto_route_library.git
      ref: hotfix/flutter_2.2.3
      path: auto_route_generator/
  build_runner: ^2.0.1
  freezed: ^0.14.2
  json_serializable: ^4.1.1
  lint: ^1.5.3

flutter:
  uses-material-design: true
